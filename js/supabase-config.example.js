/**
 * Plantilla de configuración PÚBLICA de Supabase.
 *
 * Copia este archivo como `supabase-config.js` y completa los valores locales.
 * `supabase-config.js` está ignorado por Git.
 *
 * Solo se admite una Publishable key (`sb_publishable_...`) o la clave anon
 * pública heredada. Nunca uses aquí `sb_secret_...` ni `service_role`.
 */
window.LIBRERIA_SUPABASE_CONFIG = Object.freeze({
    url: "TU_SUPABASE_PROJECT_URL",
    anonKey: "TU_SUPABASE_PUBLISHABLE_KEY"
});
